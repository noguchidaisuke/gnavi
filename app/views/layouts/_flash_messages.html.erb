<% unless flash.empty? %>
  <script>
      <% flash.each do |type, messages| %>
      <% type = type.to_s.gsub('danger','error').gsub('notice','info') %>
      toastr["<%= type %>"]("<%= messages %>", "<%= type.upcase %>");
      <% end %>
  </script>
<% end %>